import { FixedPricePipe } from './fixed-price.pipe';

describe('FixedPricePipe', () => {
  it('create an instance', () => {
    const pipe = new FixedPricePipe();
    expect(pipe).toBeTruthy();
  });
});
